# FinScore Analyzer - Example Environment Variables (Supabase)
# Copy to .env.local (for local dev) or configure in your hosting environment.

# --- NextAuth ---
NEXTAUTH_URL=http://localhost:3000
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=replace-with-a-strong-secret

# --- Supabase ---
# Public client config (safe to expose on client)
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Server-side only (never expose on client)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
